// 3. Ler um vetor de 10 elementos e mostrar em ordem contrária

#include <stdio.h>

int main(){
    int vetor_normal[10], vetor_inverso[10], i;

    for (i = 0; i < count(vetor_normal); i++)
    {
        printf("Digite um numero: ");
        scanf("%d", vetor_normal[i]);
    }

    for (i = 10; i > 0; i--)
    {
        /* code */
    }
    
    
}